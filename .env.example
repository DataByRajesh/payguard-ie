# Copy this file to .env and adjust as needed.
# Points at the local Postgres started by `pnpm db:local:start` (docker-compose.yml).
DATABASE_URL="postgresql://payguard:payguard@localhost:5432/payguard_dev"
