# Copy this file to .env for interactive local development (`pnpm dev`).
# Identical to .env.example today — kept as a separate file per the project's environment-file
# convention (local / test / production each have their own template) so future local-only
# overrides (feature flags, local-only integrations) have an obvious home that isn't .env.example.
DATABASE_URL="postgresql://payguard:payguard@localhost:5432/payguard_dev"

# See .env.example for SESSION_SECRET / SEED_USER_PASSWORD -- same local-dev defaults apply here.
